[security-infisical]
requires = ["db-postgres", "db-valkey"]

[genai-bifrost]
requires = ["db-postgres"]

[genai-openwebui]
requires = ["db-postgres", "db-qdrant"]
wants = ["genai-bifrost", "genai-embedding", "genai-ollama"]

[genai-librechat]
requires = ["db-postgres"]
wants = ["genai-bifrost", "genai-embedding", "genai-ollama"]
