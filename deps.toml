[security-infisical]
requires = ["db-postgres", "db-valkey"]

[genai-bifrost]
requires = ["db-postgres"]

[genai-openwebui]
requires = ["db-postgres", "db-qdrant"]
wants = ["ai-bifrost", "ai-embedding"]

[genai-librechat]
requires = ["db-postgres"]
wants = ["ai-bifrost", "ai-embedding"]
