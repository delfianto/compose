[dependencies.genai-bifrost]
requires = ["database"]

[dependencies.genai-openwebui]
requires = ["database"]
wants = ["genai-bifrost", "genai-embedding", "genai-ollama"]

[dependencies.genai-librechat]
requires = ["database"]
wants = ["genai-bifrost", "genai-embedding", "genai-ollama"]
