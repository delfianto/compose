services:
  valkey-mcp:
    image: mcp/valkey-server:latest
    container_name: valkey-mcp
    restart: "no"
    environment:
      - VALKEY_URL=valkey://valkey:6379
    networks:
      - database

networks:
  database:
    external: true
