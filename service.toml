[ai-bifrost]
requires = ["db-postgres", "db-valkey"]

[ai-librechat]
requires = ["db-postgres", "ai-bifrost", "ai-embedding"]
wants = ["ai-ollama"]

[ai-openwebui]
requires = ["db-postgres", "ai-bifrost", "ai-embedding"]
wants = ["ai-ollama"]

[infra-forgejo]
requires = ["db-postgres"]

[infra-infisical]
requires = ["db-postgres", "db-valkey"]

[media-immich]
requires = ["db-postgres", "db-valkey"]
