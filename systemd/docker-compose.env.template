# Docker Compose directories
DOCKER_DATA_DIR=${data_base_dir}
DOCKER_PROJ_DIR=${proj_base_dir}

# Traefik ACME configuration (for Let's Encrypt SSL)
TRAEFIK_ACME_DOMAIN=${acme_domain}
TRAEFIK_ACME_EMAIL=${acme_email}
TRAEFIK_ACME_SERVER=${acme_server}

# Optional: Build progress format (auto, plain, tty)
# DOCKER_PG_FORMAT=auto

# Optional: Custom ps format
# DOCKER_PS_FORMAT=table {{.Name}}\t{{.State}}\t{{.Status}}\t{{.Ports}}
